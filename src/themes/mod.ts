/**
 * Pre-built {@link ThemeSchema} definitions ready to use with
 * {@link generateThemeCss} or {@link generateThemedCss}.
 *
 * Each theme exports a complete light + dark mode token schema built
 * from the bundled Tailwind v3 color palette.
 *
 * @module
 */

export type { ThemeSchema } from "../types.ts";

/** Blue primary / orange accent theme. */
export { default as blueOrange } from "./blue-orange.ts";
/** Cyan primary / red accent theme. */
export { default as cyanRed } from "./cyan-red.ts";
/** Cyan primary / slate accent theme. */
export { default as cyanSlate } from "./cyan-slate.ts";
/** Emerald primary / amber accent forest theme. */
export { default as emeraldAmberForest } from "./emerald-amber-forest.ts";
/** Emerald primary / pink accent theme. */
export { default as emeraldPink } from "./emerald-pink.ts";
/** Fuchsia primary / emerald accent theme. */
export { default as fuchsiaEmerald } from "./fuchsia-emerald.ts";
/** Neutral gray monochrome theme. */
export { default as gray } from "./gray.ts";
/** Indigo primary / amber accent theme. */
export { default as indigoAmber } from "./indigo-amber.ts";
/** Lime primary / fuchsia accent neon theme. */
export { default as limeFuchsiaNeon } from "./lime-fuchsia-neon.ts";
/** Monokai-inspired theme with cyan accent. */
export { default as monokaiCyan } from "./monokai-cyan.ts";
/** Monokai-inspired theme with green accent. */
export { default as monokaiGreen } from "./monokai-green.ts";
/** Monokai-inspired theme with pink accent. */
export { default as monokaiPink } from "./monokai-pink.ts";
/** Orange primary / pink accent sunset theme. */
export { default as orangePinkSunset } from "./orange-pink-sunset.ts";
/** Pink primary / emerald accent theme. */
export { default as pinkEmerald } from "./pink-emerald.ts";
/** Pink primary / teal accent theme. */
export { default as pinkTeal } from "./pink-teal.ts";
/** Purple primary / yellow accent theme. */
export { default as purpleYellow } from "./purple-yellow.ts";
/** Multi-color rainbow theme. */
export { default as rainbow } from "./rainbow.ts";
/** Red primary / blue accent theme. */
export { default as redBlue } from "./red-blue.ts";
/** Red primary / cyan accent theme. */
export { default as redCyan } from "./red-cyan.ts";
/** Red primary / sky accent theme. */
export { default as redSky } from "./red-sky.ts";
/** Red primary / sky accent / slate base theme. */
export { default as redSkySlate } from "./red-sky-slate.ts";
/** Rose primary / teal accent theme. */
export { default as roseTeal } from "./rose-teal.ts";
/** Sky primary / amber accent theme. */
export { default as skyAmber } from "./sky-amber.ts";
/** Slate primary / cyan accent theme. */
export { default as slateCyan } from "./slate-cyan.ts";
/** Slate primary / teal accent ocean theme. */
export { default as slateTealOcean } from "./slate-teal-ocean.ts";
/** Neutral stone monochrome theme. */
export { default as stone } from "./stone.ts";
/** Stone primary / orange accent earth theme. */
export { default as stoneOrangeEarth } from "./stone-orange-earth.ts";
/** Teal primary / rose accent theme. */
export { default as tealRose } from "./teal-rose.ts";
/** Violet primary / lime accent theme. */
export { default as violetLime } from "./violet-lime.ts";
/** Violet primary / rose accent dusk theme. */
export { default as violetRoseDusk } from "./violet-rose-dusk.ts";
/** Neutral zinc monochrome theme. */
export { default as zinc } from "./zinc.ts";
